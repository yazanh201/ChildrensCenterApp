<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="#FAFAFA">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- כותרת -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="✨ הוספת פעילות חדשה"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="#444"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="24dp" />

        <!-- שם פעילות -->
        <EditText
            android:id="@+id/etName"
            android:hint="שם הפעילות"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:drawable/edit_text"
            android:padding="12dp"
            android:layout_marginBottom="12dp"/>

        <!-- תחום -->
        <Spinner
            android:id="@+id/spinnerDomain"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp" />

        <!-- מדריך -->
        <Spinner
            android:id="@+id/spinnerGuide"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp" />

        <!-- תיאור -->
        <EditText
            android:id="@+id/etDescription"
            android:hint="תיאור הפעילות"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:drawable/edit_text"
            android:padding="12dp"
            android:layout_marginBottom="12dp"/>

        <!-- גיל מינימלי -->
        <EditText
            android:id="@+id/etMinAge"
            android:hint="גיל מינימלי"
            android:inputType="number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:drawable/edit_text"
            android:padding="12dp"
            android:layout_marginBottom="12dp"/>

        <!-- גיל מקסימלי -->
        <EditText
            android:id="@+id/etMaxAge"
            android:hint="גיל מקסימלי"
            android:inputType="number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:drawable/edit_text"
            android:padding="12dp"
            android:layout_marginBottom="12dp"/>

        <!-- ימים בשבוע -->
        <EditText
            android:id="@+id/etDays"
            android:hint="ימים (למשל: ראשון, שלישי)"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:drawable/edit_text"
            android:padding="12dp"
            android:layout_marginBottom="12dp"/>

        <!-- מספר משתתפים -->
        <EditText
            android:id="@+id/etMaxParticipants"
            android:hint="מספר משתתפים מקסימלי"
            android:inputType="number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:drawable/edit_text"
            android:padding="12dp"
            android:layout_marginBottom="12dp"/>

        <!-- Switch לפעילות חד-פעמית -->
        <Switch
            android:id="@+id/switchOneTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="פעילות חד-פעמית"
            android:layout_marginBottom="24dp"
            android:textSize="16sp" />

        <!-- כפתור שמירה -->
        <Button
            android:id="@+id/btnSave"
            android:text="💾 שמור פעילות"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="#4CAF50"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:padding="12dp"/>
    </LinearLayout>
</ScrollView>